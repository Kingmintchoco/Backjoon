#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main(){
    int t, n, tmp;
    string str;

    cin >> t;
    while(t--){
        cin >> str >> n;

        vector <int> v(n);
        for(int i = 0; i < n; ++i){
            cin >> v[i];
        }
    }
}